Use list comprehension instead of bulky map and filter calls (later)
Make specification for smoke tests (later)
Actually create smoke tests (later)
Consider custom character escaping (later)
Installation guides? (later)
[s,5] looks bad mixing 98.2 and 100M for instance (later)
Customize metadata output? (later)
to_human is duplicated in audiodir.py (later)
Really hardcode charset in html output?
